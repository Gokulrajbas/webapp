apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
spec:
  serviceAccountName: nginx-sa  # Specify the service account
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
